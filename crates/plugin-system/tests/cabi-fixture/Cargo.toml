[package]
name = "cabi-fixture"
version = "1.2.3"
edition = "2021"
description = "Test fixture: a C-ABI ('c-flat') plugin used by plugin-system integration tests"
publish = false

[lib]
crate-type = ["cdylib"]

[dependencies]
serde = { version = "1", features = ["derive"] }
serde_json = "1"
